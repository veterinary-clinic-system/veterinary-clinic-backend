/**
 * API cong khai cua bounded context `triage` (phan loai muc do khan cap bang AI).
 *
 * Module khac CHI duoc import tu day (hoac tu `domain/entities`). Xem ghi chu
 * trong `modules/notification/application/index.ts`.
 */
export { PrescreeningService } from './prescreening.service';
